# 流程配置器（第一版）

这是一个只监听本机 `127.0.0.1` 的可视化流程编辑器。Web 页面负责编辑和保存 JSON 工作流，本机 Python Agent 在当前 Windows 登录会话中使用 `pywinauto`、鼠标键盘、截图和 RapidOCR 执行步骤。

## 启动

双击：

```text
启动流程配置器.cmd
```

启动后浏览器会自动打开：

```text
http://127.0.0.1:8765
```

运行任务前需要保持 HIS 窗口已经打开。执行过程中不要锁屏、切换 Windows 用户或操作鼠标键盘。

## 当前能力

- 点击左侧模块，将模块添加到流程；
- 拖动模块或使用上下箭头调整执行顺序；
- 修改步骤名称、启用状态和模块参数；
- 配置 HIS 文本输入、下拉项选择和勾选框状态；
- 按自定义对象名称点击按钮、页签、菜单项或其他 UIA 控件；
- 每个 OCR 步骤可独立配置定位方式、表格标题、目标列、截图范围和表格边界；
- 保存、重新加载 JSON 配置；
- 在“配置页”集中管理多个配置，支持新建、编辑和删除；
- 基于现有安全动作新增、修改和删除自定义操作模块；
- 运行完整流程，或运行到选中的步骤；
- 查看步骤级实时日志和最终输出路径；
- 运行日志区域可以收起或展开；
- 同一时间只允许一个桌面任务运行；
- 正在运行的任务可以请求取消；等待查询、等待页面和普通等待会立即响应取消，短暂的截图/OCR 动作会在当前动作完成后停止；
- 配置只能调用注册过的模块，不能执行任意 Python 代码。

## 标准 HIS 流程顺序

“配置页”中提供了 `标准-HIS住院号医嘱取证`，正确顺序为：

1. 连接 HIS 窗口；
2. 最大化 HIS 窗口；
3. 填写住院号并回车；
4. 等待患者查询结果；
5. 点击第一条查询结果；
6. 等待医嘱页面；
7. 截取当前位置；
8. 截取横向最右端；
9. OCR 标注审核字段。

“发送按键”“按窗口比例点击”“点击 UIA 控件”“等待”属于高级可选模块。上述专用 HIS 模块已经包含必要的输入、回车、点击和等待逻辑，标准流程中不需要重复添加这四个模块。

默认配置保存在：

```text
configs\default.json
```

截图和 OCR 标框图片默认保存到：

```text
..\output
```

## 主要文件

- `server.py`：本机 HTTP Agent 和配置接口；
- `workflow_engine.py`：JSON 校验、变量替换、顺序执行和任务状态；
- `rpa_modules.py`：可供页面编排的安全模块库；
- `web/`：可视化页面；
- `configs/default.json`：当前 HIS 医嘱取证流程。

## 变量

模块参数支持 `${变量名}`。默认流程使用：

```text
${hospitalization_number}
```

页面顶部的“本次住院号”会覆盖该变量，因此无需修改 Python 文件即可切换患者。

## 版本回溯

代码版本库位于 `scratch` 目录。每轮修改前先提交当前可运行状态，修改并测试通过后再提交新版本；`output`、日志和 Python 缓存不会进入版本库，避免运行截图或患者信息被纳入代码历史。

查看版本记录：

```text
cd /d D:\AI\AI_S\系统模拟demo\scratch
git log --oneline --decorate
```

需要回退时，先停止流程配置器并确认当前代码改动是否需要保留，再选择目标版本。为防止误覆盖未保存工作，建议把目标版本号交给维护人员执行回退。

## 后续扩展

新增能力时，在 `rpa_modules.py` 中实现处理函数并注册 `ModuleDefinition`。页面会通过 `/api/bootstrap` 自动取得新模块及其参数定义，不需要为每个模块重新编写前端页面。
